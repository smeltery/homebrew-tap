# gh-identity

Multi-account GitHub identity management with context-based account switching and per-directory identity binding.

```bash
brew install --cask smeltery/tap/gh-identity
```

Installs `gh-identity` and `gh-identity-hook`.

- Upstream: [smeltery/gh-identity](https://github.com/smeltery/gh-identity)
- Issues: [smeltery/gh-identity/issues](https://github.com/smeltery/gh-identity/issues)
